import { MultipicaporPorPipe } from './multipicapor-por.pipe';

describe('MultipicaporPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipicaporPorPipe();
    expect(pipe).toBeTruthy();
  });
});
